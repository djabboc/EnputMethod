Fix BUG :

一、假如已经输入几个选项，并且激活输入框窗口以后，如果通过左右切换光标位置，进行修改，将会输入错误！

例如已经输入 hel，假如移动左键想要插入一个字母s，例如 he\<s>l，将会得到hel\<s>&#x20;

这是什么原因导致的？

二、如果输入 he 然后输入 \<?> 这个问号，最后输入一个空格

在某些软件里面（例如记事本），将会得到 \<?>he< >，显然这是错误的，因为光标似乎跳到了开头位置？

但是在 ChatGPT 聊天窗口里面似乎又会得到  he< >\<?>，当然此时也是错误的，因为空格出现在了错误的位置。

这又是什么原因导致的？和上面的原因相同吗？

增加功能：

一、增加快捷键 shortcut.json 配置文件；一个动作可以对应多个快捷键；目前已经有几个默认的快捷键：例如 Tab 可以选择第一个选项。

二、目前只能最多显示9个选项，需要能够翻页，最终将所有匹配的选项全部能够遍历一次（默认快捷键 - 和 + 前翻页和后翻页）

三、增加精准选择功能：默认通过 up arrow 和 down arrow 选择一个选项。到底以后自动往下翻页，到顶以后自动往上翻页。

四、可以配置选中项的样式（通过主题文件）


编写开发任务书；每次完成一个任务；每次都要验证，通过以后及时提交代码


------

安装完成后，切换到 Enput Method - English，按以下顺序测试。每项回复“是”或“否”即可：
1. Notepad 输入 hel，按左方向键一次，再输入 s。
   预期：文本为 hesl，不会出现重复、覆盖或光标跳位。
2. 新行输入 he、?、空格。
   预期：文本为 he? ，问号和空格都在 he 后。
3. 输入 th，候选超过一页时按 +，再按 -。
   预期：窗口页码变化，返回后显示原页；输入框中的 th 不变。
4. 输入 th，按两次下方向键，再按 Tab。
   预期：Tab 提交当前高亮项，并按默认设置追加一个空格。
5. 观察高亮候选。
   预期：高亮随上下键移动，且显示主题配置的选中背景、文字和边框。
请先回复安装是否成功，然后按 1-5 分别回复“是”或“否”。

1. 失败：输入 hel 以后，左键导致选词框消失

2. 失败：输入 hel 以后，输入 ? 导致选词框消失

3. 成功：可以来回切换（小问题：每次我按下按键切换，选词框都会有非常短暂的窗口闪烁）

4. 成功:Tab 提交当前高亮项，并按默认设置追加一个空格。  

5. 成功：高亮随上下键移动，且显示主题配置的选中背景、文字和边框。  （小问题：每次我按下按键切换，选词框都会有非常短暂的窗口闪烁）

------

将本次我们如何共同完成安装验证的这个流程写进文档固化，便于后续开发和维护，我不想每次都重新对 Agent 提相同的要求。

p.s. 为什么安装提示我们提前切换到其他输入法？处于避免占用的原因？