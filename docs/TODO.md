# Enput Method 待办记录

> 本文是早期阶段任务的归档。当前统一的状态与人工验收以 `docs/development-issue-ledger-zh-CN.md` 为准。

## 阶段 1：配置约定

- [x] 确定公开配置文件名：保留 `conf.json` 或迁移至 `config.json`。
  - 保持向后兼容，不丢失已有用户配置。
  - 记录最终位置 `%LOCALAPPDATA%\Enput Method`。
- [x] 将默认候选数从 `4` 改为 `9`。
  - 将值限制在键盘可选择范围 `1` 至 `9`。
- [x] 新增并校验以下配置字段：
  - `candidateCount`：默认 `9`。
  - `layout`：`vertical` 或 `horizontal`；默认 `vertical`。
  - `appendSpaceAfterSelection`：布尔值；默认 `true`。
  - `fontFamily`：`Segoe UI`。
  - `fontSize`：`18` 点；WPF Overlay 会转换为 24 个设备无关像素。
  - `opacity`：`1.0`。
  - `theme`：默认 `dark`。
- [x] 用可安全拒绝无效值并回退默认值的 JSON 解析器，替换早期能力有限的配置读取器。

## 阶段 2：候选框行为

- [x] 默认竖排渲染候选，配置后可横排。
- [x] `appendSpaceAfterSelection` 启用时，数字选词后自动补一个空格。
  - 被选单词后紧接标点，或用户执行不应获得额外空格的显式动作时，不附加空格。
- [x] 将配置的字体系列、字号和不透明度应用到悬浮候选框。
- [x] 保持窗口不抢焦点，定位于 TSF 组合输入光标，并可用 `1` 至 `9` 选择。
- [x] 本地学习候选选择频率，但不改变完整匹配优先级或未学习词典条目的顺序。
- [~] 连续选词、退格、Enter、Escape、Space、标点、光标移动、无匹配输入、翻页，以及在记事本、EmEditor 和 ChatGPT 中输入。
  - 许多个别场景已通过用户验证。剩余跨应用回归和最新鼠标翻页修复已列入 `docs/development-issue-ledger-zh-CN.md` 统一验证。

## 阶段 3：主题

- [x] 创建包含四个内置 JSON 主题文件的 `themes/` 文件夹：
  - `dark.json`（默认）
  - `light.json`
  - `eye-care.json`
  - `paper.json`
- [x] 定义背景、前景、边框、选中行颜色与边框、边框宽度、圆角、内边距、行高和阴影的主题字段。
- [x] 从用户配置中的 `theme` 加载主题，失败时安全回退到 `dark`。
- [x] 设计候选框渲染器，使主题同时控制颜色和形状而不改变输入逻辑。

## 阶段 4：词典与发布

- [x] 保持 `dictionary.txt` 可由用户编辑，并记录其“每行一个单词、顺序决定优先级”的格式。
- [x] 增加带实用词频排序的较大默认英文词典。
- [x] 确保安装器更新不覆盖已有用户配置、词典或自定义主题文件。
- [x] 更新安装器、卸载器、README、中文更新说明、架构说明和发布验证步骤。

## 完成条件

- [~] 全新安装创建默认配置、词典和四套主题。
  - 已验证已有安装的升级行为；全新安装回归仍在统一验收清单中。
- [x] 修改配置无需重新构建输入法，即可改变候选数、布局、自动空格、字体、字号、不透明度和主题。
- [x] 默认深色竖排候选框显示九个带编号的选择项，数字选词后补空格。
- [~] 所有变更通过发布构建、COM 激活、已安装 DLL 验证，以及记事本和 EmEditor 的多次选词手工测试。
  - 发布构建和已安装 DLL 检查已通过。当前人工状态与剩余用例维护在 `docs/development-issue-ledger-zh-CN.md`。