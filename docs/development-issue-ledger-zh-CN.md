# 开发问题总账与集中验收清单

最后更新：2026-08-29。

本文件是开发期间所有已发现问题、已作修复、尚待验证限制和明确延后决策的唯一汇总入口。这里的“已修复”表示代码已完成、构建已通过并有独立提交；除非标为“用户已验证”，否则不能视为人工验收通过。

## 已关闭且用户已验证

| 编号 | 问题与现象 | 根因与处理 | 状态 |
| --- | --- | --- | --- |
| I-01 | 旧 DLL 存在或仍被应用加载时，安装可能失败。 | 安装路径曾固定为 `.8.dll`，且复制不能复用已存在文件。改为按 DLL 时间戳和大小部署版本化路径；旧应用仍可保留旧 DLL。 | 用户已验证安装成功；提交 `3120b96`。 |
| I-02 | 输入 `hel` 后左移，候选框消失或插入 `s` 后文本错误。 | 未接管的导航键改变了目标应用的选区，而 TSF 组合范围仍处于活动状态。导航和编辑键先完成组合，再按原语义交给应用；组合内左右移动改为更新 TSF 选择。 | 用户已验证；提交 `c28f65a`、`1f5e405`。 |
| I-03 | 输入 `he`、`?`、空格后，`?` 丢失、候选框提前结束或文字顺序错误。 | 可打印标点被错误地作为未处理按键传递，和活动组合发生竞争。按当前键盘布局把可打印字符直接提交到组合。 | 用户已验证 `he? `；提交 `1f5e405`。 |
| I-04 | 候选框在按键、长按边界键、翻页或高亮变化时闪烁。 | 窗口反复显示/隐藏、整窗重绘和新旧图像帧暴露。改为保留位置、局部失效、双缓冲、原子打开和透明旧帧处理。 | 用户已验证基本消除；提交 `38d9aa4`、`98b3aa0`、`7d11d84`、`83b24b2`、`900811d`。 |
| I-05 | Emoji 模式在切换候选时有旧候选短暂残留，首次从 `smile` 切到 `c` 有中间态。 | 彩色字体渲染需要连续绘制区域，旧层窗口像素未先清除。改为整行连续绘制并在新帧前透明化旧像素。 | 用户已验证；提交 `7d11d84`、`83b24b2`、`900811d`。 |
| I-06 | Emoji 候选显示黑白、没有英文提示、字体和普通候选大小不一致。 | GDI 不支持彩色字体；候选没有可见关键词；Emoji 字体度量未按主题字号统一。使用 DirectWrite 彩色字体并为每项显示可选标签，统一字号。 | 用户已验证；提交 `5d25bb4`、`d9f6335`、`dd17dcc`。 |
| I-07 | 上下切换时候选整体下移；翻到第 2 页时窗口比第 1 页低。 | 选择和翻页重新依据会延迟稳定的应用组合矩形定位。导航期间固定当前候选窗坐标。 | 用户已验证；提交 `dcd923c`、`669e59f`、`a9f9ceb`。 |
| I-08 | 页脚 `Prev`/`Next` 与页码重叠，CAPS 与页码或三位页码被截断。 | 页脚宽度按候选区估算，未独立测量标记、页码和导航所需空间。页脚按实测文字宽度布局，CAPS 与导航分区。 | 用户已验证；提交 `f71735c`、`b4fdb6c`。 |
| I-09 | 完整单词没有优先显示；`Ent` 得到全小写；Caps Lock 和按住 Shift 的大小写不一致。 | 候选排序未单独保留精确匹配，大小写仅按 Caps Lock 简化处理。精确匹配优先；`preserveCase` 按实际输入处理；Caps Lock 下 Shift 反转为小写候选，同时保留 CAPS 标记。 | 用户已验证；提交 `f39891e`、`0f0e34e`。 |
| I-10 | 鼠标点选非首项时只保留原输入 `he`；点选后没有下一个联想词。 | 鼠标回调异步编辑会话未携带被点候选；提交后过早关闭建议状态。回调捕获候选文本，选择后进入零长度的后续建议状态。 | 用户已验证；提交 `a3bb812`、`8197bd3`。 |
| I-11 | 选择 `how` 后再选 `how are you` 得到 `how how are you`。 | 关联短语作为完整文本提交，而前一个词已经写入目标应用。关联短语以已提交词为前缀时，只提交后缀。 | 用户已验证；提交 `2b763d7`。 |
| I-12 | 输入时卡死。 | 为短语去重增加了对大词库的嵌套线性扫描，短前缀退化为二次复杂度。改用大小写无关哈希集合。 | 用户已验证不再卡死；提交 `ed1b706`。 |
| I-13 | 翻译框覆盖候选框。 | 两个非激活窗口都锚定在输入光标下方。翻译框改为根据候选窗边界放在其侧方。 | 用户已验证；提交 `9dcae58`。 |
| I-14 | 默认配置、快捷键、分页、精确高亮、主题选中项、边缘避让、连续联想、Emoji、翻译开关、Tab/数字/Enter/Escape/Backspace 等功能缺失。 | 按独立任务实现配置文件、主题、候选状态、关联数据和窗口。 | 各功能已由用户分项验证，详见任务书与提交历史。 |
| I-15 | `abandoned` 可翻译而 `abandon` 无翻译，尽管完整词典中存在 `abandon`。 | 文件二分查找从中间位置向后跳过整行；当区间仅剩目标行时会误读下一行并结束。改为回溯到包含中间位置的当前行再比较。 | 算法修复和当前回归均通过；完整 ECDICT 结果仍列入人工抽样。 |
| I-16 | `abandon` 的完整释义超过翻译窗高度，底部内容无法查看；内容未超过默认高度时无法验证滚动。 | 翻译视图已迁移为 WPF ScrollViewer，并保留主题最大高度约束；长释义会获得滚动区域。 | 自动化构建通过；真实应用中的长释义滚动待验收。 |

## 已修复、待集中手工验收

| 编号 | 项目 | 当前状态 | 集中验收方法 |
| --- | --- | --- | --- |
| V-01 | 鼠标页脚翻页与悬停反馈 | WPF 三段式页脚已替代旧实现；自动化覆盖边缘位置，用户已确认悬停高亮。 | 输入 `t` 使候选超过一页。悬停 `<`、`>` 应为手型且仅对应按钮高亮；点击 `>`/`<` 翻页，不把字符写入底层应用、不抢焦点、不移动候选窗。 |
| V-02 | 完整英中词典首次下载与查询 | 已实现，提交 `5983741`；首次安装下载 ECDICT CSV 并生成本地 JSONL。 | 在网络可用的首次安装后，输入非演示词如 `abandon`，按 F3；应显示英文定义、中文释义和词性。 |
| V-03 | 发布回归 | `0be9934` 的 Release 构建（0 警告、0 错误）、原生测试、Overlay 协议/WPF 自动化与系统级安装验证均已通过。 | 重开 Notepad、EmEditor、VS Code、浏览器和 ChatGPT，确认实际输入法为 Enput 后执行本文件末尾的完整矩阵。 |

## 已知限制与明确延后项

| 编号 | 项目 | 原因与后续边界 |
| --- | --- | --- |
| D-01 | 横向布局的导航轴 | 已确认合理目标：纵向为上下选择、左右翻页；横向为左右选择、上下翻页，快捷键跟随布局。用户明确要求暂不修改；任务见 `pending-layout-navigation-zh-CN.md`。 |
| D-02 | 日文完整词典 | 当前仅有示例日文映射。完整英日数据应使用 JMdict，并遵循 CC BY-SA 4.0 的署名和相同许可要求；不能把 ECDICT 伪称为完整日文词典。 |
| D-03 | 翻译数据完整性 | `translations.json` 是用户可覆盖的小型 JSON 词典；ECDICT 提供大规模英中覆盖，但不保证每个词都有完整例句或所有语言映射。未下载、下载失败或转换失败时仍会回退到小词典。 |
| D-04 | 应用更新生效条件 | TSF DLL 在目标应用进程内加载。更新后仅切换输入法不能保证换用新 DLL，必须关闭并重开目标应用；安装提示先切换其他输入法是刷新 Profile 可见性，不是解除 DLL 占用的唯一手段。 |
| D-05 | 最终体验判定 | 候选窗口属于跨应用 TSF UI，视觉闪烁、焦点和鼠标命中必须在真实应用中验收；静态构建不能替代人工验证。 |

## 集中验收顺序

安装最新版本后，先切换到其他输入法再切回 Enput Method，并重开每个待测应用。每项记录“通过”或实际文本/窗口行为。

1. `hel` 左移、输入 `s`，预期 `hesl`，候选窗不消失。
2. `he`、`?`、空格，预期 `he? `。
3. 输入 `t`，用 `+`、`-` 与鼠标 `<`、`>` 往返分页；前缀不提交，窗口位置稳定，鼠标按钮有手型和高亮。
4. 在首项长按上键、在第一页长按 `-`，再进行正常翻页；不应有明显闪烁或整体位移。
5. 鼠标选择非首项，预期完整候选写入；若有后续关联，候选窗继续显示下一个词/短语。
6. 输入 `how` 后选择 `how`，再选择 `are you`，预期 `how are you`，不重复 `how`。
7. F2 输入 `smile`、`cry`，预期彩色 Emoji、英文提示、统一字号、稳定切换；选择后写入 Emoji。
8. F3 依次高亮 `hello`、`hell`、`abandon`，预期翻译窗在候选窗旁、随高亮更新；`abandon` 验证 ECDICT 数据。
9. 分别输入 `enter`、`Ent`，以及开启 Caps Lock 和 Caps Lock + Shift；候选大小写符合实际输入，CAPS 标记不重叠页码。
10. 恢复默认配置后验证纵向 9 项；再按自定义配置验证横向 3 项、Consolas 18、透明度、Tab 不追加空格。横向导航轴现状只记录，不作为本轮通过条件。
11. 在 Notepad、EmEditor、ChatGPT 各重复第 1、2、3、5 项。

## 关联文档

- `development-tasks-zh-CN.md`：原始任务拆分和早期验证记录。
- `advanced-input-tasks-zh-CN.md`：高级功能任务拆分与数据边界。
- `installation-validation-zh-CN.md`：安装、UAC 与 DLL 一致性流程。
- `dictionary-sources-zh-CN.md`：ECDICT 和后续日文数据的来源与许可。


## 2026-08-29：WPF Overlay 重构后问题全录

本节补充 I-01 至 I-16 之后的全部已知问题。状态严格区分：**用户已验证** 是真实输入法宿主中的结果；**自动化通过** 只代表构建、协议或受控 WPF 宿主通过；**待人工验证** 不能因安装成功而关闭。

| 编号 | 问题与现象 | 根因与处理 | 当前状态 |
| --- | --- | --- | --- |
| I-17 | 重构后候选框完全消失，数字选词逻辑仍正常。 | 仅迁移了视图而未稳定交付完整 Overlay 状态，原生候选与 WPF 显示路径混用。改为 C++ 发布完整候选批次，WPF-only UI，保留原生状态直至 Overlay 确认呈现。 | 自动化通过；后续基本候选显示已人工观察。 |
| I-18 | 候选出现后立即无效/消失，甚至使记事本卡顿。 | 管道确认、候选与翻译消息的时序会阻塞按键或让旧状态覆盖新状态。管道 I/O 改为异步有界队列，状态批次原子发布，拒绝过期 `stateId`。 | 自动化协议与宿主测试通过；跨应用压力待验收。 |
| I-19 | 输入后删除全部字符，候选框仍留在屏幕上。 | 空候选更新未映射为对应 Surface 的隐藏事件。 | 已修复；`hide` 和空候选路径自动化通过。 |
| I-20 | 同时打开两个记事本时存在两个候选窗，失焦窗口仍可鼠标选词。 | Overlay 没有按 Host 隔离窗口，也没有前景所有者仲裁。每个 `clientId` 维护独立窗口，仅前景编辑器显示并接收操作。 | 前景仲裁自动化通过；真实双应用切换待验收。 |
| I-21 | 鼠标候选/翻页可能激活窗口，或动作回到错误 Host。 | WPF 窗口缺少 `WS_EX_NOACTIVATE` 约束，动作未绑定源管道连接。 | 非激活样式和源管道路由自动化通过。 |
| I-22 | 安装时 Overlay 正在运行导致部署失败或新 UI 未更新。 | Overlay 文件被占用，安装器未管理其生命周期。安装前停止已安装 Overlay，并围绕活跃 Host 重试部署；安装后比较所有文件。 | 自动化安装验证通过。 |
| I-23 | 已安装且自动输入了前缀，但测试实际上使用微软输入法。 | 自动化只能模拟按键，不能证明 Windows 当前选择的是 Enput。 | 作为测试边界记录；选择 Enput 与真实文本结果仍须人工确认。 |
| I-24 | 自适应候选排序影响词典默认顺序，无法关闭。 | 本地频率学习没有配置开关。新增 `adaptiveCandidateRanking`，设为 `false` 时保持词典顺序并停止写入。 | 构建和逻辑测试通过；配置行为待人工确认。 |
| I-25 | F2 Emoji 模式不能稳定启用，或候选缺少模式提示。 | Overlay 未完整接收模式和主题状态。协议增加模式标记，WPF 渲染 `EMOJI` 指示。 | 自动化协议通过；基础 Emoji 查询已人工观察。 |
| I-26 | F3 翻译框覆盖候选框。 | 翻译窗和候选窗使用相同的光标下方锚点。翻译窗依据候选窗屏幕边界放在相邻侧，屏幕边缘时受工作区约束。 | 实现与构建通过；最终视觉位置待人工确认。 |
| I-27 | 候选框弹出有数百毫秒延迟。 | 日志证明冷态耗时发生在 WPF 收到候选消息后的首次窗口创建，而热态通常只需数毫秒。Overlay 在进程启动的空闲阶段于屏幕外、无激活地预热候选窗，并复用于第一个 Host；找不到 Overlay 管道时的连接重试降为 25ms，安装更新期间仍保持 250ms 退避。 | 已构建、安装并有无可见预热自动化；待真实冷启动量化。 |
| I-28 | Emoji 候选显示黑白字形，且关键词标签/字号不统一。 | WPF 使用系统 Emoji 字体会受宿主字体与彩色字形支持影响。改用安装包内 Twemoji PNG 色彩图标，保留关键词标签。 | 色彩资源自动化通过；用户已观察到彩色显示。 |
| I-29 | `fire` 等基础关键词没有候选，尽管 JSON 中存在条目。 | 安装器的 `System.Text.Json` 将非 BMP Emoji 写成 `\\uD83D\\uDD25`；原生读取器把高低代理项分开编码，得到无效 UTF-8。合并代理对并增加 `🔥` 回归测试。 | **用户已验证 `fire -> 🔥`**；提交 `765492e`。 |
| I-30 | Emoji 目录太少，`fire`/`water`/`bucket`/`cat`/`dog` 等基础词不易优先出现。 | 原始目录和关键词覆盖有限、没有基础项优先级。扩充颜色资产和目录，为基础词设置较高优先级，安装时合并用户数据。 | 构建/安装通过；完整召回率待人工抽样。 |
| I-31 | `<` 固定后 `>` 仍会随长候选跑到中间；翻页按钮位置和可点击范围不稳定。 | 导航控件作为内容组布局，受候选文本宽度和指示器影响。改为整行三段式：左箭头贴左边缘，页码在可用区域居中，右箭头贴右边缘；可用时悬停高亮。 | Overlay 自动化通过；悬停已由用户确认。 |
| I-32 | 配置显示 `fontSize=18`，视觉上仍接近 16。 | 原生将数值解释为 pt，WPF `FontSize` 却解释为 DIP，18 DIP 仅约 13.5pt。Overlay 统一转换为 `pt * 96 / 72`，18pt 渲染为 24 DIP。 | 新增自动化断言并已安装；待用户视觉确认。 |
| I-33 | F3 翻译内容把 `\\n` 原样显示，`block` 等长释义不换行。 | ECDICT JSONL 有双重转义的字面量换行；JSON 解析后仍是反斜杠加字母。服务端在构造翻译视图前规范化 `\\n`、`\\r`、`\\r\\n`。 | 原生回归测试和安装通过；待用户在新宿主验证 `block`。 |
| I-34 | 翻译正文显示 `,"source":"CC-CEDICT ..."`，用户误以为没有释义。 | 服务端曾把内部 `source` 字段拼入 `content`。删除 UI 输出，继续在安装归属文件与数据文档保留许可。 | 自动化协议与构建通过；待真实宿主验收。 |
| I-35 | 日语翻译总是出现，即使用户没有选择日语。 | 翻译消息无条件枚举所有语言。新增 `translationLanguages`，默认 `["en", "zh-CN"]`；仅配置中显式加入 `ja-JP` 时显示日语。 | 自动化配置/协议通过；待真实宿主验收。 |
| I-36 | `braces` 等义项的 `n.` 不稳定，`{{or}}` 被直接显示。 | ECDICT 原始文本部分行首词性没有句点，且含模板标记。仅规范已知词性前缀并去除精确标记 `{{or}}`，不做会吞掉普通标点的泛化替换。 | 原生回归通过；待真实宿主验收。 |
| I-37 | `hug` 看似只显示来源，没有英中内容。 | 已安装 ECDICT 原始记录包含 `n. a tight or amorous embrace` 和“紧抱、拥抱”等；根因是 I-34 的来源正文与旧宿主 DLL/Overlay 组合。 | 代码修复与数据抽样确认；待关闭重开宿主后验收。 |
| I-38 | 翻译窗尺寸不能由用户调整，文本布局不适合词性、语言和例句。 | 原实现是 `TextBlock + ScrollViewer` 且 `SizeToContent`。改为只读 `RichTextBox + FlowDocument`、四边/四角无激活命中测试、`config.json` 初始尺寸和 250ms 防抖写回。 | WPF 自动化通过；待真实鼠标缩放与持久化验收。 |
| I-39 | `saw -> 🪚` 在 VS Code 变方框，Saint Helena Flag 视觉上被误认作瑞士。 | 服务端提交 `U+1FA9A` 正确；方框由目标编辑器字体缺字决定。Saint Helena 正确为 `🇸🇭`（区域指示符 S/H），瑞士为 `🇨🇭`（C/H）。清理重复关键字并增加码点回归。 | 码点和安装数据测试通过；VS Code 字体兼容性需按宿主单列观察。 |
| I-40 | SQLite 迁移后 F2 无 Emoji、F3 无翻译，普通英文候选部分可用。 | `enput.db` 已生成并删除 JSON/JSONL，但已打开的 Notepad 仍映射旧 JSON 版 TSF DLL；旧 DLL 可继续读取保留的 `dictionary.txt`，却找不到已删除的 Emoji/翻译文件。安装并注册 SQLite DLL 后关闭重开宿主即可。 | 已安装新 DLL，SQLite 自检通过；F2 `fire` 与 F3 `braces`/`hug` 待重开真实宿主后验收。 |
| I-41 | 翻译窗使用富文本但无法鼠标选择、复制，且主题只能统一设置正文颜色。 | RichTextBox 被禁用文档交互；主题协议缺少语义 Run 的样式字段。开放只读文档交互并提供右键 Copy；将词性、标签、例句的颜色和例句背景纳入每套主题。 | Overlay 自动化覆盖结构、选择与复制菜单；手工复制验收见 `translation-candidate-polish-tasks-zh-CN.md`。 |
| I-42 | Emoji 模式中 `-`、`+` 可能未由 TSF 优先捕获，且严格前缀不能容忍省略字符或 Emoji 关键字分隔符。 | Emoji 有候选时显式捕获配置的分页键；在前缀匹配之后追加首字符一致、长度至少 3 的 SQLite 保序查询，Emoji 比较忽略 `_`、`-`、空白。 | 原生单元覆盖 `hpy`、`pignose` 和反序拒绝；Release 构建通过，实际宿主翻页待手工验收。 |
| I-43 | `pignose` 和 `empirestate` 不能分别找到 `pig_nose` 与 Empire State Building。 | 已确认 `pig_nose` 存在于 SQLite 且 SQL 查询可命中，故需部署新 TSF；短语表缺少 Empire State Building，补为内置短语，并将无空格保序匹配扩展到 suggestions。 | 单元与 SQLite 验证覆盖；本轮安装后在真实宿主复测。 |
| I-44 | `install-and-verify.ps1` 在 SQLite 验证实际成功后仍报告失败。 | PowerShell 对 GUI 子进程的 `&` 调用不保证填充 `$LASTEXITCODE`。改为 `Start-Process -Wait -PassThru` 并检查显式 `ExitCode`。 | 修复后重新执行系统安装与 SQLite 验证。 |

### 当前自动化证据

截至提交 `0be9934`，已实际执行并通过：Release 安装包构建（C++ TSF、WPF Overlay、安装器均为 0 警告、0 错误）、原生数字选词/词典标记/Emoji 码点测试、10 个 Overlay 协议用例、多 Host 管道路由、前景仲裁、彩色 Emoji 资产、翻页边缘布局、18pt 字号换算、富文本翻译视图、翻译尺寸配置，以及“用户缩放后旧服务端消息不得覆盖尺寸”回归。`scripts\\install-and-verify.ps1 -Configuration Release` 已完成系统级安装验证，确认注册的 TSF DLL、已部署 Overlay、默认翻译语言、尺寸配置、`🪚` 与 Saint Helena 码点、CC-CEDICT 索引和归属文件一致。

### 当前人工验收清单

安装新版本后，关闭并重新打开每一个目标应用，再确认已切换至 Enput Method：

1. 输入 `he`：候选显示为更大的 18pt 视觉字号，`<`、页码和 `>` 分别位于左、中、右区域。
2. 按 F2 输入 `fire`、`water`、`bucket`、`cat`、`dog`：候选应为彩色 Emoji，数字键可提交。
3. 按 F3 输入 `braces`、`hug`、`block`：仅显示英文与中文，没有 `source` 正文，`n.` 与真实换行正确，翻译窗不覆盖候选窗。
4. 拖动翻译窗任意边或角，隐藏后重新显示并切换候选：尺寸应保持，并在 `%LOCALAPPDATA%\Enput Method\UserData\config.json` 写回 `translationWindowWidth`、`translationWindowHeight`。
5. 在配置中加入 `"ja-JP"` 后重按 F3：才显示日语；删除该项后不显示日语。
6. 在 VS Code 输入 F2 `saw`：若出现方框，记录 VS Code 的编辑器字体和 Windows Emoji 字体版本；提交文本必须仍是 `U+1FA9A`。
7. 同时打开两个记事本，在两个窗口各输入前缀并来回切换焦点：仅前景窗口可见且可操作，背景窗口不得响应鼠标。
8. 在 Notepad、EmEditor、VS Code、浏览器和 ChatGPT 复测组合内编辑、标点、数字选词、鼠标选词、连续联想、翻页和失焦隐藏。
9. 冷启动后记录从首次输入到候选出现的时间；如果持续超过可接受阈值，应单列性能任务，不与正确性问题混合。


## 2026-08-29：活动组合文本下划线的决策

| 编号 | 现象与结论 | 当前决策 |
| --- | --- | --- |
| D-06 | 输入 `hello` 后未选词时，编辑区域下方出现下划线。Enput 将未确认文本保留为活动 TSF `composition`；目标应用根据该状态绘制下划线。服务当前没有设置 `ITfDisplayAttribute` 或下划线颜色、粗细等视觉属性，因此外观由记事本、编辑器或浏览器决定。 | 暂不修改。保留活动 composition，数字选词可以原子替换该范围。若要“候选框仍存在但无下划线”，必须将候选状态从 composition 解耦，并重新设计未确认文本、选词替换范围、光标移动与跨应用兼容性；这不是单纯隐藏样式。 |

| I-45 | 常见地名和跨学科短语召回不足，例如 `newyork`、机器学习、合同法。 | 原 suggestions 只覆盖少量手工高频续写，没有系统性多词语料。下载官方 WordNet 3.1，提取 62,319 条二至五词 lemma 导入 SQLite，并补充高优先级术语。 | Release 构建、SQLite、无空格匹配回归和系统安装验证通过；用户已确认成功。 |
| I-46 | 用户习惯按 Shift 终止联想，但此前只支持 Escape。 | 取消动作被硬编码为单个 Escape 键且修饰键会直接放行。新增 `shortcut.json.cancelComposition` 数组，默认 Escape/Shift；TSF 在组合状态下优先捕获两者并执行同一取消路径。 | 自动化、安装验证通过；用户已确认成功。 |
| I-47 | 输入 `hello` 后按 Tab，出现 `world` 等后续联想时，Escape 和 Shift 被吞掉却不能关闭候选。 | Tab 已提交并结束 active composition，服务转入 detached follow-up suggestion；取消分支仍调用 `FinishComposition`，因没有 composition 而返回 `S_FALSE`，未清理候选状态。 | 取消分支按状态路由：detached 建议只清理自身并隐藏窗口，不写编辑区；鼠标 dismiss 同样复用该路径。新增 Escape、左右 Shift 和取消路由的原生回归，Release 构建/安装验证通过；真实 Notepad 验收待当前 UI 自动化环境恢复或用户复测。 |

## 2026-08-29：一站式发布改造记录

| 编号 | 问题/疑惑 | 结论与处理 | 验证状态 |
| --- | --- | --- | --- |
| P-01 | 发布目录能否在安装后任意删除？ | 采用方案 A：注册 DLL、Overlay 和静态资源部署到 `C:\Program Files\Enput Method`；发布目录只是安装介质。Windows 注册表不指向解压目录，因此安装后可移动/删除该目录。 | 本地包与发布 ZIP 结构检查通过；系统安装路径由安装验证检查。 |
| P-02 | 为什么“直接从解压目录注册”不能自由删除？ | 若采用方案 B，注册表会指向解压目录中的 TSF DLL，目录移动/删除会留下失效 COM 路径。该方案未采用。 | 架构决策已文档化。 |
| P-03 | 安装/卸载点击后看似卡住。 | 两个 WPF 启动器改为后台任务、阶段文本、进度条和禁用重复点击；安装覆盖部署、注册、SQLite 初始化、验证，卸载只覆盖注销与注册表验证，不停止 Overlay 或删除产品根。 | 卸载器 Release 构建 0 错误/0 警告；GUI 进度的真实 UAC 观察待系统安装验收。 |
| P-04 | 静态资源与用户设置混在 AppData。 | 静态资源统一部署到 Program Files；仅配置、快捷键和日志留在 `UserData`。升级不覆盖用户文件，卸载默认保留它们。 | payload 完整性、UAC 安装、Program Files 路径、SQLite 自检和现有用户配置/快捷键 hash 保留均通过。 |
| P-05 | 打包校验首次失败。 | 主题被打进 `Resources` 根目录，运行时实际从 `Resources\themes` 读取。拆分 MSBuild item 并将主题固定到 `payload\Resources\themes`。 | 重建本地 Release 包后 `--verify-package` 通过。 |

## 2026-08-29：发布包安装/卸载事故

| 编号 | 现象 | 根因 | 处理与状态 |
| --- | --- | --- | --- |
| P-06 | 从本地 Release 包启动卸载器，提示“文件不完整、不匹配或已占用”。 | 实际 payload DLL 可加载且两个卸载导出存在；Program Files 中有 Explorer、ChatGPT、VS Code、Edge 等多个宿主映射 Enput TSF DLL，递归删除阶段抛出文件占用异常。旧 UI 将此误报为包不完整。 | 卸载器改为自动行高和阶段化日志；最终语义改为只注销 Profile/分类/COM 注册，不删除 DLL、Overlay 或资源，因此不再需要关闭宿主或重启。无界面注销与随后立即重装已通过。 |
| P-07 | 安装器显示 `0x80004002`，包检查日志又覆盖了安装错误。 | 单一 `install-verification.log` 被 `--verify-package` 和 `--verify-lexicon` 复用；本地包构建的增量路径还可能暂存旧 TSF DLL。 | 发布脚本改为 `/t:Rebuild`；新增 native registration stage 导出，安装器显示具体 TSF 阶段；日志拆分为 install/package/lexicon 三份。新 payload 已验证包含诊断导出。 |
| P-08 | 原有微软拼音看似失效。 | 许多已启动应用仍加载 Enput 的 in-process TSF DLL，且 Enput profile 与微软拼音同属中文输入法组；这不等同于删除微软拼音注册。 | 已将默认输入法恢复为 Microsoft Pinyin；必须重启 Windows 后验证所有进程均已卸载旧 DLL。微软拼音的用户语言项仍存在。 |
| P-09 | 安装器日志报“拒绝访问”，但自动脚本没有失败；随后多个安装实例争用 `enput.db.pending`，且注册 TSF 后 Overlay 可能重新占用数据库。 | 无界面 WPF 仅设置 `Environment.ExitCode` 未传给 `Shutdown`；安装锁只覆盖 Overlay 复制，未覆盖词库；词库准备位于 TSF 注册之后且临时名固定。 | 无界面退出码改为真实传播；全安装互斥；词库先于 TSF 注册；GUID staging；有效旧数据库保留并跳过重复下载。2026-08-29 UAC 安装、静态资源和 SQLite 验证通过。 |
| P-10 | 手工点击安装器在“TSF 分类”阶段失败，Enput 曾出现在输入法列表，随后中文输入法会话异常；重启后半注册项消失。 | GUI 将原生注册放入 `Task.Run` 的 MTA 线程；原生层忽略 `RPC_E_CHANGED_MODE`，继续在错误单元模型中注册。分类 `TF_E_ALREADY_EXISTS` 也未按幂等成功处理，且 Profile 在分类成功前就被启用，失败没有完整回滚。 | GUI/无界面统一改用专用 STA 工作线程；STA 初始化失败立即返回；键盘分类幂等、immersive 可选、Profile 延后启用；失败清理 Enput 自己的 CTF TIP/COM/Profile/分类。真实 UAC 卸载、干净状态重装和 SQLite 验证已通过。 |

## 2026-08-30：重点复盘 - 近一小时 TSF 安装事故

本轮从用户报告“卸载成功但旧宿主仍可输入、重装在 TSF 分类阶段失败、微软中文输入法异常”到用户确认最终成功，实际排查、修复、构建和真实 UAC 复验接近一小时。这一耗时不应被视为普通安装波动：失败安装曾留下半注册 TIP，并对当前 Windows TSF 会话产生可见影响。以下内容作为发布与调试的强制经验记录。

### 事故链

1. 用户先卸载 Enput。已打开应用继续使用内存中映射的旧 TSF DLL，这是“只注销注册、不删除运行文件”策略的预期行为，不代表卸载失败。
2. 用户从 WPF 安装器点击安装。GUI 原本用 `Task.Run` 调用原生 TSF 注册，线程池为 MTA；TSF Profile 和 Category 注册却要求 STA。
3. 原生层收到 `RPC_E_CHANGED_MODE` 后没有立即停止，继续注册 COM、Profile 和语言项，随后在核心键盘分类阶段失败。Profile 曾被提前启用，故 Enput 会短暂出现在输入法列表。
4. 半注册 TIP 使当前 TSF/ctfmon 会话异常，用户看到微软中文输入法似乎“死掉”；重启 Windows 后会话缓存重建，半注册项被隐藏，微软拼音恢复。这不是代码删除了微软拼音注册表。
5. 早期无界面 `--install-and-verify` 从 WPF 启动 STA 执行，曾能成功，从而错误地给出“安装正常”的信号；它没有覆盖 GUI 的 MTA 路径。旧自动化也没有把卸载和紧随其后的重装纳入同一条回归链。

### 已固化的修复

- 安装器按钮、卸载器按钮及两个无界面入口统一经专用 STA 工作线程调用原生 TSF；STA 初始化失败在任何注册前直接返回。
- TSF 安装按事务处理：先注册服务和 Profile，先保证核心键盘分类，再启用 Profile；`TF_E_ALREADY_EXISTS` 是可重试的幂等结果，immersive 分类仅为可选能力。
- 任一安装步骤失败都会清理 **仅属于 Enput** 的 Profile、分类、HKCU/HKLM `CTF\\TIP\\{9C8945D5-...}` 根和 CLSID，并保留最初的失败阶段/HRESULT。代码绝不遍历、删除或改写 Microsoft Pinyin 的 `{81D4E9C9-...}`。
- 卸载成功不再只相信原生返回码；卸载器会检查 Enput 的 COM 与 TIP 根确实不存在。DLL、Overlay、静态资源和用户配置刻意保留，让已打开宿主不会因缺依赖崩溃，也支持不重启立即重装。
- 安装成功也不再只检查 COM DLL：还必须看到 Enput 中文 LanguageProfile 与核心键盘分类，否则向用户报告失败。

### 经验与教训

1. **自动化必须走与用户相同的执行路径。** “无界面成功”不能证明“点击 GUI 成功”；线程单元模型、同步上下文和 COM 初始化均可能不同。共享一个可测的 STA 执行器后，两个入口才有可比性。
2. **Windows 输入法注册不是一组可随意重试的独立写入。** COM、Profile、Category 和启用状态必须作为事务；任何中途失败都必须回滚 Enput 自己的全部入口，且启用动作应放到最后。
3. **“旧应用仍能工作”与“新安装可以使用”是两个状态。** 卸载时保留被映射 DLL 是正确的兼容策略；判断卸载完成应检查未来激活入口是否移除，不能要求用户关闭所有应用，更不能为了删除文件破坏当前宿主。
4. **错误信息和日志必须属于本次操作。** GUI 失败必须覆盖写安装日志；包校验、词库校验和 TSF 安装日志不可复用，否则旧成功日志会掩盖真实失败。
5. **发布验收必须验证完整生命周期。** 当前 `scripts/run-regression.ps1` 的强制顺序是“构建 -> Overlay/原生测试 -> 安装 -> 无界面卸载验证 -> 不重启立即重装”。卸载验证同时检查 Enput 注册清理、共享文件与用户配置保留、以及非 Enput 输入法项未变化。
6. **必须诚实记录测试边界。** 现有回归已覆盖真实 STA 正常链路和立即重装，但尚未有完全确定性的“强制核心 Category 注册失败”注入测试。后续应增加只在测试环境显式启用的失败钩子或隔离集成环境，验证失败回滚和微软拼音基线保持不变；不得通过修改系统注册表 ACL 人为制造故障。

### 本次可复用结论

对于当前已发布的修复，用户不需要在每次安装/卸载后重启；真实 UAC 链路已经验证可以立即重装。只有旧版半注册状态已经让某个已打开应用持有异常 TSF 会话时，关闭该应用或重启一次才是清除历史缓存的恢复手段，而不是新版安装流程的常规要求。

## 2026-08-30: Translation-window selection regression

- Symptom: a F3 translation window could render rich text and expose Copy, but mouse drag did not create a usable selection.
- Cause: WS_EX_NOACTIVATE preserves the editor's TSF focus, while the default RichTextBox selection route tries to acquire keyboard focus.
- Fix: explicit preview-mouse selection maps drag coordinates to TextPointer ranges; right-click opens Copy without clearing the range; scrollbar input remains native.
- Verification: Overlay automation uses rendered text coordinates to select text, checks that keyboard focus remains outside the overlay, and confirms the Copy menu exists.
