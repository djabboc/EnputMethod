# Enput Method 开发任务书

## 目标

修复活动 TSF 组合文本与应用光标不同步导致的编辑错误，并增加可配置快捷键、完整候选分页、键盘精确选择和选中项主题样式。

## 执行规则

每个任务必须独立完成以下步骤后才能开始下一项：

1. 完成实现与相关文档。
2. 执行该任务的构建和验收测试。
3. 记录验证结果。
4. 创建仅包含该任务的 Git 提交。

## 验证记录

| 任务 | 结果 | 记录 |
| --- | --- | --- |
| 任务 1 | 构建通过 | 2026-08-27：Visual Studio MSBuild 已通过 `Release|x64` 全量构建，零警告、零错误。需要安装的 Notepad 与 ChatGPT 手工回归留在任务 5 的发布验证中，以避免在开发时更改当前系统输入法注册。 |
| 任务 2 | 构建和产物检查通过 | 2026-08-27：`Release|x64` 全量构建零警告、零错误；安装器输出目录含默认 `shortcut.json`。 |
| 任务 3 | 构建通过 | 2026-08-27：`Release|x64` 全量构建零警告、零错误；代码检查确认所有匹配词保留在独立列表中，当前页按 `candidateCount` 切片，翻页不提交组合文本。 |
| 任务 4 | 构建通过 | 2026-08-27：`Release|x64` 全量构建零警告、零错误；代码检查确认上下移动在候选页边缘切换相邻页，默认 Tab 提交当前高亮项，四套默认主题均含选中项边框字段。 |
| 任务 5 | 构建和分发资源检查通过；手工验收待执行 | 2026-08-27：最终 `Release|x64` 全量构建零警告、零错误；安装器输出含 `config.json`、`shortcut.json`、`dictionary.txt` 和主题文件。由于安装会更改当前系统输入法注册，Notepad、EmEditor 和 ChatGPT 的安装后手工验证尚未执行。 |
| 任务 6 | 通过 | 2026-08-27：`Release|x64` 全量构建零警告、零错误；在旧 `.8.dll` 存在时安装成功。注册表指向新的时间戳和大小版本化 DLL，且已部署 DLL 的 SHA-256 与安装器内 DLL 一致。 |
| 任务 7 | 通过 | 2026-08-27：用户验证左移时候选窗保持显示，后续输入得到正确文本；候选窗闪烁已有改善。 |
| 任务 8 | 通过 | 2026-08-27：用户验证 `he`、`?`、空格得到 `he? `；页首长按上键、第一页长按 `-`、以及正常下翻页均无明显闪烁。 |

## 任务 1：组合文本与光标一致性

### 问题与根因

输入 `hel` 后按左右方向键，或输入 `he` 后按标点再按空格，未被服务接管的按键会直接交给目标应用；但服务自身的 TSF 组合文本仍保持活动。应用修改选择范围或插入标点后，服务在下一次按键中依据已过期的组合范围重新写入 `typed_`，造成文本被覆盖、光标位置错误或空格顺序错误。

### 实现范围

- 活动组合文本遇到导航、标点、编辑或快捷键按键时，先在同步编辑会话中提交组合文本。
- 提交后不吞掉原按键，让目标应用按原语义处理它。
- 保留已有字母输入、候选选择、退格、Tab、空格、Enter 和 Escape 行为。

### 验收

- 在 Notepad 和 ChatGPT 输入框中输入 `hel`、左移一次、输入 `s`，结果为 `hesl`。
- 输入 `he`、`?`、空格，结果为 `he? `。
- 两次连续候选选择、退格、Escape、Enter 与无匹配输入不回归。

## 任务 2：快捷键配置

### 实现范围

- 新增 `%LOCALAPPDATA%\\Enput Method\\shortcut.json`，由安装器仅在缺失时创建。
- 一个动作支持多个快捷键，并在文件无效时回退到默认配置。
- 初始动作至少包含当前项选择（默认 `Tab`）、上一页（默认 `-`）和下一页（默认 `+`）。旧版 `selectFirst` 配置仍被兼容为当前项选择。

### 验收

- 默认文件可用。
- 一个动作的两个不同快捷键都可触发。
- 无效 JSON 与无效按键定义不会使输入法失效。

## 任务 3：完整候选分页

### 实现范围

- 保留所有与前缀匹配的候选词，窗口只渲染当前页。
- 候选数为每页数量；`-`、`+` 默认前后翻页。
- 页首、页尾、无匹配与单页场景有确定行为。

### 验收

- 候选超过每页数量时能访问最后一项。
- 数字键选择当前页对应项目。
- 翻页不会丢失输入前缀或组合文本。

## 任务 4：精确选择和主题选中态

### 实现范围

- 默认向上、向下键移动当前选中候选。
- 页底继续向下进入下一页首项；页首继续向上进入上一页末项。
- 主题支持选中项背景、前景及边框样式；窗口渲染实际选中项而非固定首项。

### 验收

- 上下键和跨页循环的选中项正确。
- Tab/数字快捷键提交当前目标项目的规定行为。
- 四套默认主题及自定义主题的选中项样式生效。

## 任务 5：发布验证与文档

### 实现范围

- 更新 README、架构说明、中文说明和待办状态。
- 按发布流程验证安装、更新、卸载和两类目标应用。

### 验收

- Release x64 构建成功。
- 安装器创建缺失的默认配置、词库、主题和快捷键文件，且不覆盖用户文件。
- 所有手工输入场景通过并记录结果。

## 任务 6：DLL 部署名称

### 问题与根因

部署路径被固定为 `EnputMethod.Tsf.8.dll`，安装时使用禁止覆盖的复制方式。即使文件没有被加载，只要旧文件存在，新的构建也无法复制，安装器返回失败。

### 实现范围

- 使用 DLL 的写入时间和大小生成稳定的版本化文件名。
- 同一二进制文件重复安装时验证内容一致后复用已部署文件。
- 新构建使用新路径，避免被仍加载旧 DLL 的应用阻塞。

### 验收

- 旧 `.8.dll` 存在时，新构建能够安装。
- 同一构建重复安装不会因目标文件已存在而失败。

## 任务 7：组合内光标与候选窗更新

### 实现范围

- 左右方向键移动活动组合文本内的 TSF 选择位置，不结束组合。
- 字母和退格按组合内光标位置插入或删除。
- 候选窗仅在可见性、位置或尺寸变化时重新显示和定位。

### 验收

- 输入 `hel` 后左移再输入 `s`，结果为 `hesl`，且左移时候选窗保留。
- 翻页和上下高亮时不存在明显的显示/隐藏闪烁。

## 任务 8：标点提交与重复按键重绘

### 实现范围

- 在当前键盘布局下，将活动组合文本后的可打印按键直接提交到组合文本。
- 保持导航和非文本快捷键交还给目标应用。
- 页首、页尾或当前高亮不变时不重绘候选窗。

### 验收

- 输入 `he`、`?`、空格得到 `he? `。
- 按住上键或 `-` 到达边界时，候选窗不再反复闪烁。


## 后续重构与验证记录（2026-08-29）

候选窗和翻译窗已从进程内原生 UI 迁移至独立 WPF Overlay；C++ TSF 服务仍独占组合、按键处理、候选状态、坐标查询和文本提交。迁移后新增并通过的自动化覆盖包括：JSON Lines 协议、同机多 Host 路由、过期状态动作拒绝、前景应用仲裁、空候选隐藏、安装文件一致性、彩色 Emoji、翻页按钮边缘布局以及 18pt 字号换算。

本日的 Release 回归和系统安装验证通过。用户已验证 `fire -> 🔥`。18pt 的视觉大小和 `F3` 输入 `block` 时的实际翻译换行，仍应在关闭并重开目标应用后作最后人工确认；不得将自动化安装结果误记为 Windows 已切换到 Enput 的证明。
