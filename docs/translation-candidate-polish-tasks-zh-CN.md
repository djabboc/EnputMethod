# 翻译与候选体验完善任务书

日期：2026-08-29。范围覆盖翻译悬浮界面的主题与复制交互、Emoji 分页以及保序不完整匹配；不改变 SQLite 专用词库边界，也不把翻译窗口尺寸从 `config.json` 移入主题。

## 任务与验收

| 任务 | 实现 | 自动验收 | 状态 |
| --- | --- | --- | --- |
| 翻译主题富文本 | 每套主题定义词性、语言标签、例句前景和例句背景；正文用 `FlowDocument` 的独立 `Run` 呈现词性、标签、释义与例句。 | Overlay 自动化检查结构化段落、语义标签和主题字段。 | 已完成 |
| 翻译复制 | 翻译窗口保持不抢焦点；标题栏 `Copy` 按钮复制完整翻译，成功后短暂显示 `✓ Copied`。不支持鼠标拖选文本，避免改变正在输入的编辑器焦点。 | Overlay 自动化检查剪贴板写入、成功反馈和反馈自动恢复。 | 已完成 |
| Emoji 键盘翻页 | Emoji 状态且存在候选时，TSF 优先捕获配置的 `-`、`+`、小键盘减/加键；分页只改变页码，不提交组合文本。 | TSF 按键路径与完整发布构建。 | 已完成 |
| 保序不完整匹配 | 长度至少为 3、首字符一致时，以 SQLite `LIKE` 初筛并在 C++ 再校验顺序；普通词、Emoji 关键字和短语候选均参与。前缀与精确匹配始终排在其前。Emoji/短语匹配忽略 `_`、`-` 和空白。 | 原生单元用例覆盖 `hpy -> happy`、`pignose -> pig_nose`、`empirestate -> empire state building`、反序拒绝和 LIKE 转义；安装验证检查词库数据。 | 已完成 |

## 排序与性能规则

1. 精确匹配、短语续写、前缀匹配、保序不完整匹配依次合并。
2. 自适应频率排序只在各自层级内部生效，不能把近似结果提升到前缀结果之前。
3. 近似查询只在输入至少三个字符后运行，并受首字符范围和 96 条上限约束，避免每次按键扫描整个词库。
4. Emoji 精确关键字、前缀关键字、保序关键字依次排序，然后才使用 Emoji 词条优先级。

## 手工验收

1. 在记事本切换 Enput，按 F2 输入 `s` 或其他可产生两页以上结果的关键字，按 `+` 到下一页、`-` 回上一页；文本不应被提交。
2. F2 输入 `pignose`，候选中应包含 `pig_nose` 所关联的 Emoji；普通模式输入 `hpy`，候选中应出现 `happy`，输入 `empirestate` 应出现 `empire state building`，且前缀候选仍在近似结果前。
3. 按 F3 显示 `braces`，确认词性、`en:`、`zh-CN:`、例句使用不同富文本样式；点击标题栏 `Copy`，在记事本粘贴后内容应与完整翻译一致，按钮应短暂显示 `✓ Copied`。
4. 更换 `%LOCALAPPDATA%\Enput Method\themes` 中的主题或字段后重开候选/翻译窗，确认相应富文本颜色更新；窗口宽高仍由 `config.json` 的 `translationWindowWidth`、`translationWindowHeight` 控制。

## 2026-08-29：词组语料与取消动作

- [x] 下载并导入公开 WordNet 3.1 多词短语，保留归属文件。
- [x] 补充高频地名和跨学科术语：经济、商业、心理、计算机、工程、法律。
- [x] 为无空格短语输入增加 `newyork` 和 `machinelearning` 回归。
- [x] 新增 `cancelComposition` 可配置快捷键数组，默认 `Escape` 与 `Shift`。
- [x] 真实宿主人工复验：确认 Enput 已激活后，验证上述两个短语和 Shift 取消；用户已确认成功。